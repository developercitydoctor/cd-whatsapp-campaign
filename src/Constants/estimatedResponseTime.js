/**
 * Global estimated response time (seconds) - same value used across the app.
 * Random integer between 30 and 70, set once when the module loads.
 */
// export const ESTIMATED_RESPONSE_TIME_SEC =
//   30 + Math.floor(Math.random() * (70 - 30 + 1));

export const ESTIMATED_RESPONSE_TIME_SEC = 30;